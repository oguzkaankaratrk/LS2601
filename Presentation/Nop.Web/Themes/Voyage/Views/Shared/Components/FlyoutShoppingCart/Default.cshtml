@model MiniShoppingCartModel
@inject IWorkContext workContext
@inject ICustomerService customerService
@inject Nop.Core.IWebHelper webHelper
@using Nop.Core.Domain.Catalog
@using Nop.Core
@using Nop.Services.Customers
@{
    var returnUrl = Context.Request.Query.TryGetValue("returnUrl", out var url) && !StringValues.IsNullOrEmpty(url)
                ? url.ToString()
                : webHelper.GetRawUrl(Context.Request);
    var customerIsRegistered = false;
    var currentCustomer = await workContext.GetCurrentCustomerAsync();
    if (currentCustomer != null)
    {
        if (await customerService.IsRegisteredAsync(currentCustomer))
        {
            // Do Something
            customerIsRegistered = true;
        }
    }
}
@* 
<div class="flyout-cart-wrapper">
    <div class="cart-link">
        @if (customerIsRegistered)
        {
            <img style="padding-left: 25px;padding-top: 3px;" src="@Url.Content($"~/Themes/Voyage/Content/svg/header/user.svg")" />
            <a style="display:block;" href="@Url.RouteUrl("CustomerInfo")" class="ico-account"> @T("Account.MyAccount")</a>
            //<li><a href="@Url.RouteUrl("CustomerInfo")" class="ico-account">@T("Account.MyAccount")Model.CustomerName</a></li> 
        }
        else
        {
            <img style="padding-left:7px" src="@Url.Content($"~/Themes/Voyage/Content/svg/header/user.svg")" />
            <a style="display:block;" href="@Url.RouteUrl("Login", new { returnUrl })" class="ico-user">@* @T("Account.Login") Sign In</a>
        }

    </div>
</div> *@
<div class="flyout-cart-wrapper">
    <div class="cart-link">
        <div class="Wishlist-svg">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="currentColor" class="bi bi-heart" viewBox="-1 -3 18 18">
                <path d="m8 2.748-.717-.737C5.6.281 2.514.878 1.4 3.053c-.523 1.023-.641 2.5.314 4.385.92 1.815 2.834 3.989 6.286 6.357 3.452-2.368 5.365-4.542 6.286-6.357.955-1.886.838-3.362.314-4.385C13.486.878 10.4.28 8.717 2.01zM8 15C-7.333 4.868 3.279-3.04 7.824 1.143q.09.083.176.171a3 3 0 0 1 .176-.17C12.72-3.042 23.333 4.867 8 15" />
            </svg>
        </div>
        <div class="Wishlist-Text">
            <a style="display:block;" href="@Url.RouteUrl("Wishlist", new { returnUrl })" class="ico-login">@* @T("Account.Favorites") *@Favorites</a>
        </div>
    </div>
</div>
<div class="flyout-cart-wrapper" id="flyout-cart" data-removeItemFromCartUrl="@Url.Action("RemoveItemFromCart", "VoyageTheme")" data-flyoutCartUrl="@Url.Action("FlyoutShoppingCart", "VoyageTheme")">
    <div id="topcartlink" class="cart-link">
        <a href="@Url.RouteUrl("ShoppingCart")" class="ico-cart">
            <div class="cart-svg">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" class="bi bi-bag" viewBox="0 0 16 16" style="padding-left:7px;">
                    <path d="M8 1a2.5 2.5 0 0 1 2.5 2.5V4h-5v-.5A2.5 2.5 0 0 1 8 1m3.5 3v-.5a3.5 3.5 0 1 0-7 0V4H1v10a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V4zM2 5h12v9a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1z" />
                </svg>
            </div>
            <div class="cart-text">
                <span class="cart-label">@T("SevenSpikes.Themes.Voyage.HeaderShoppingCartText")</span>
                <span class="cart-qty">@Model.TotalProducts</span>
            </div>
        </a>
    </div>
    <div class="flyout-cart-dropdown">
        <div class="mini-shopping-cart">
            @if (Model.TotalProducts == 0)
            {
                <div class="no-data">
                    @T("ShoppingCart.Mini.NoItems")
                </div>
            }
            else
            {
                <div class="count">
                    @Html.Raw(string.Format(T("ShoppingCart.Mini.ItemsText").Text, $"<a href=\"{Url.RouteUrl("ShoppingCart")}\">{string.Format(T("ShoppingCart.Mini.Items").Text, Model.TotalProducts)}</a>"))
                </div>
            }
            @if (Model.TotalProducts > 0)
            {
                <div class="items">
                    @for (var i = 0; i < Model.Items.Count; i++)
                    {
                        var item = Model.Items[i];
                        <div class="item @(i == 0 ? "first" : null)" data-shoppingCartId="@item.Id">
                            @if (Model.ShowProductImages)
                            {
                                <div class="picture">
                                    <a href="@(Url.RouteUrl<Product>(new { SeName = item.ProductSeName }))" title="@item.Picture.Title">
                                        <img alt="@item.Picture.AlternateText" src="@item.Picture.ImageUrl" title="@item.Picture.Title" />
                                    </a>
                                </div>
                            }
                            <div class="product">
                                <div class="name">
                                    <a href="@(Url.RouteUrl<Product>(new { SeName = item.ProductSeName }))">@item.ProductName</a>
                                </div>
                                @if (!string.IsNullOrEmpty(item.AttributeInfo))
                                {
                                    <div class="attributes">
                                        @Html.Raw(item.AttributeInfo)
                                    </div>
                                }
                                <div class="quantity">@T("ShoppingCart.Mini.Quantity"): @item.Quantity</div>
                                <div class="price">@item.UnitPrice</div>
                                <div class="remove">
                                    <span class="remove-btn">@T("Common.Remove")</span>
                                </div>
                            </div>
                        </div>
                    }
                </div>
                <div class="totals">
                    <label>@T("ShoppingCart.Totals.SubTotal"):</label>
                    <strong>@Model.SubTotal</strong>
                </div>
                <div class="buttons">
                    @if (Model.DisplayShoppingCartButton)
                    {
                        <button type="button" class="button-1 cart-button" onclick="setLocation('@(Url.RouteUrl("ShoppingCart"))')">@T("ShoppingCart.Mini.ViewCart")</button>
                    }
                    @if (Model.DisplayCheckoutButton)
                    {
                        var checkoutUrl = "";
                        if (Model.AnonymousCheckoutAllowed && Model.CurrentCustomerIsGuest)
                        {
                            checkoutUrl = Url.RouteUrl("LoginCheckoutAsGuest", new { returnUrl = Url.RouteUrl("ShoppingCart") });
                        }
                        else
                        {
                            checkoutUrl = Url.RouteUrl("Checkout");
                        }
                        <button type="button" class="button-1 checkout-button" onclick="setLocation('@checkoutUrl')">@T("Checkout.Button")</button>
                    }
                </div>
            }
        </div>
    </div>
</div>